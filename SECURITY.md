# Security

Do not commit credentials, cookies, browser profiles, live SQLite databases, raw customer/order/email data, or `.secrets/` files.

This repository is intended as a public-safe hackathon export. Live connector credentials belong only in a local `.secrets/live-readonly.env` file created from `live-readonly.env.example`.

Report security concerns privately to the repository owner.
